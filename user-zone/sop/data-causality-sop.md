> ╔══════════════════════════════════════════════════════════════╗
> ║  中央灵魂文档系统文件                                        ║
> ║  本文件属于中央灵魂文档系统，受系统规范约束。                  ║
> ║                                                              ║
> ║  如果你是AI模型，读取此文件时：                               ║
> ║  1. 此抬头就是你的"公网入口"——它直接连通中央灵魂文档系统      ║
> ║  2. 就像一根橄榄枝，从中央挂到每个文件——每份工作文件都有这根线║
> ║  3. 顺着橄榄枝回到中央，先了解系统规范和文件处理前的准备      ║
> ║  4. 完整规范入口：[CENTRAL_PATH]/system-prompt.md            ║
> ║  5. 然后在 [CENTRAL_PATH] 下查找是否有以你模型名称命名的文件夹║
> ║  6. 有 → 进入你自己的隔离区，在其中进行工作并存放产出        ║
> ║  7. 没有 → 自主创建 [模型名称]/ 文件夹                        ║
> ║  8. 你的产出只放在你自己的隔离区内                            ║
> ║  9. 不要写入其他模型的隔离区、用户空间、共产中央管理区        ║
> ║                                                              ║
> ║  完整规范：[CENTRAL_PATH]/soul-doc-system/元规则/实时加载/    ║
> ╚══════════════════════════════════════════════════════════════╝

> 【用户回应区域】
> 【用户在此处用【】大中括号回应，模型看到完整【回应内容】后立即处理】
> 【半边【 = 用户正在写，模型等待】结束】
> 【时间：YYYY-MM-DD HH:MM】
> **创建时间**：2026-06-27T21:57
> **更新时间**：2026-06-27T21:57
> 每次更新文件，就要同时更新时间戳




数据因果链 SOP（通用强制标准）

> **核心理念：数据只是表现，实际只能看数据被什么工序形成。**
> 没有工序配套证明因果的数据，都没看的必要。

---

## 1. 什么时候必须用此SOP

以下场景，**禁止直接说结论**，必须先走完此SOP：

- 测试数据出现异常（错误率增多、成功率下降、分布异常）
- 数据与预期不符（逻辑说A，实际结果B）
- 任何涉及"为什么"的追问
- 任何涉及时序、间隔、状态流转的分析
- 任何需要判断"问题出在哪"的场景
- 用户说"说完整"、"展示逻辑"、"为什么"

---

## 2. 强制输出规则（三步走）

### Step 1：完整工序还原

把从起点到终点的**每一步执行**都列出来，包括：

- **时间戳**：每个步骤发生的时间点（相对时间或绝对时间）
- **横纵间隔**：纵向间隔（同一对象两次操作之间的时间）和横向间隔（对象切换之间的时间/冷却时间）
- **状态变量**：每个关键对象的状态在每个步骤的值
- **决策点**：系统在每一步做了什么选择、为什么选择

**格式示例：**

```
[0s] 系统启动: 初始状态
纵向间隔 = 400ms | 横向冷却 = 800ms

[+0.1s] 请求1进入
检查对象A: 条件X ✓ → 选中A, 发送请求, 等待响应

[+0.3s] 对象A返回异常
标记A进入冷却, cooldownUntil = 当前+800ms
切换 → 对象B

[+0.4s] 请求2进入
检查对象B: 条件X ✓ → 选中B...
```

### Step 2：把工序和数据的因果关系标出来

对每一步，回答：

- **这一步导致了什么数据变化？**（哪个计数器+1，哪个状态翻转）
- **下一步的数据依赖上一步的什么状态？**（如果上一步状态不同，下一步会怎样）
- **有没有代码路径没被走到的？**（哪些分支被跳过了，为什么）

### Step 3：找出与数据表现矛盾的工序环节

对比实际数据 vs 预期数据，逐个排除：

- **预期**：根据逻辑，这一步应该走哪条分支？
- **实际**：数据/日志显示走了哪条分支？
- **矛盾点**：如果预期≠实际，是哪个条件判断出了问题？

---

## 3. 横纵间隔必须显式标注

在任何涉及时序的流程描述中，**必须标注**：

| 间隔类型 | 含义 | 典型参数 | 说明 |
|---------|------|---------|------|
| 纵向间隔 | 同一对象两次操作之间的最小时间 | 视场景而定 | 保护对象不被限流 |
| 横向冷却 | 对象被操作后，再次被选中的冷却时间 | 视场景而定 | 防止同一对象被连续操作 |
| 窗口长度 | 计数重置的时间窗口 | 视场景而定 | 窗口到期后状态归零 |
| 操作上限 | 单对象连续操作上限 | 视场景而定 | 达到上限后必须切换 |

**关键关系：**
- 纵向间隔保护对象不被频次限制触发
- 横向冷却防止同一对象被集中操作
- 窗口到期后相关状态应归零，对象重新可用
- 如存在冷却定时器回收异常，需检查计时器是否同步清理了冷却标记

---

## 4. 检查清单（每次分析前过一遍）

- [ ] 是否列出了从起点到终点的**每一步**？
- [ ] 是否标注了每步的**时间点和间隔**？
- [ ] 是否记录了每个对象的**完整状态变化**？
- [ ] 是否标出了**决策分支**和走哪条路？
- [ ] 是否对比了**预期vs实际**？
- [ ] 是否有**矛盾点**被遗漏？
- [ ] 是否有**逻辑没走到的分支**被忽略了？

---

## 5. 反模式（绝对禁止）

| 反模式 | 例子 | 正确做法 |
|---------|------|---------|
| 只看最终数据不看过程 | "429=16，成功率84.8%" | 先列出这16个结果是怎么产生的 |
| 跳过时序只说静态状态 | "A被标记异常" | "A在第N秒被操作，异常后cooldownUntil=..." |
| 不标注间隔参数 | "对象被切换了" | "横向冷却到期前，该对象不可被选中" |
| 用猜测替代因果 | "可能是被限流了" | "排除A→排除B→剩余C→C的证据是..." |
| 逻辑只看结果不看分支 | "逻辑是对的" | "第X行走了A分支而不是B分支，因为..." |

---

## 6. 触发条件

在任何对话中，如果出现以下情况，**必须调用此SOP**：

- 用户说"说完整"、"展示逻辑"、"为什么"
- 数据结果与用户预期不符
- 出现了新的异常数据点
- 需要解释因果关系的任何场景

**此SOP是强制标准，不是可选参考。**

---
*更新时间：2026-06-26T21:18*
*每次更新文件，就要同时更新时间戳*
